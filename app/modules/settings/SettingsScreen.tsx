import React, { FC } from 'react';
import { Text } from 'react-native';
import { Strings } from '../../constants';

const SettingsScreen: FC = () => <Text>{Strings.emptyString}</Text>;

export default SettingsScreen;
