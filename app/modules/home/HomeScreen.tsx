import React, { FC } from 'react';
import { Text } from 'react-native';
import { Strings } from '../../constants';

const HomeScreen: FC = () => <Text>{Strings.bottomNavigationTitles.home}</Text>;

export default HomeScreen;
